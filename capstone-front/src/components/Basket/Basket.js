import React from 'react';
import PropTypes from 'prop-types';
import './Basket.css';

const Basket = () => (
  <div className="Basket">
    Basket Component
  </div>
);

Basket.propTypes = {};

Basket.defaultProps = {};

export default Basket;
